package main

/*
func (h *Handler) CreateEvent(c *gin.Context){
	var event Event
	if err := c.BindJSON(&event); err != nil{
			fmt.Printf("Не могу распознать объект Event", err.Error())
			c.JSON(http.StatusBadRequest, ErrorRespose{
				Message: err.Error()})
		}
		return
	}

	c.JSON(http.StatusOk, "Удачно получили объект с id" + event.Id)
}*/
